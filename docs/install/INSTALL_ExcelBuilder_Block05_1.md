# Install Excel Builder Block 05.1 – Test Syntax Fix

Packa upp ZIP-filen i projektets rot och ersätt befintliga filer.

Kör:

```bat
python -m pytest
build_excel_report.bat
```

Skicka senaste ZIP-filen från:

```text
rapportzip/
```

Commit:

```bat
git add .
git commit -m "Excel Builder Block05.1 Test Syntax Fix"
git push
```
