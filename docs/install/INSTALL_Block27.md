# Install Block27 EWC Star Normalizer Fix

Packa upp ZIP-filen i projektets rot och ersätt befintliga filer.

Kör:

```bat
python -m pytest
build_report.bat
```

Commit:

```bat
git add .
git commit -m "Parser3 Block27 EWC Star Normalizer Fix"
git push
```
