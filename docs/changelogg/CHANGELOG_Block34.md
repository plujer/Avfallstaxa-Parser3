# Changelog Block34

- `tools/zip_output.ps1` excludes `output/archive` from new report ZIPs.
- Keeps archive copy in `output/archive`.
- Keeps feedback ZIP in `rapportzip`.
- Added final cleanup tests.
- No parser logic changes.
