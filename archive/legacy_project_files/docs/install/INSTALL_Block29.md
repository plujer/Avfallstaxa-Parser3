# Install Block29 Output Structure + Unit Split

Packa upp ZIP-filen i projektets rot och ersätt befintliga filer.

Kör:

```bat
python -m pytest
build_report.bat
```

ZIP-filen för återrapportering finns därefter i:

```text
docs/
```

Commit:

```bat
git add .
git commit -m "Parser3 Block29 Output Structure and Unit Split"
git push
```
