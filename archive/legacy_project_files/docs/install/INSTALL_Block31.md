# Install Block31 §6.1.4 + Output Fix

Packa upp ZIP-filen i projektets rot och ersätt befintliga filer.

Kör:

```bat
python -m pytest
build_report.bat
```

ZIP-filen för återrapportering finns därefter i:

```text
rapportzip/
```

Commit:

```bat
git add .
git commit -m "Parser3 Block31 Section 6.1.4 and Output Fix"
git push
```
