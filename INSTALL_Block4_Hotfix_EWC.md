# Block4 Hotfix EWC

Packa upp ZIP-filen i projektets rot:

```text
C:\PyProjects\Taxa 3.0
```

Verifiera:

```bat
.venv\Scripts\activate
python -m pytest
```

Commit:

```bat
git add .
git commit -m "Fix EWC metadata extraction"
git push
```
